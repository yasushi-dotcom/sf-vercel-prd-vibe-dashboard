# 2026-03-20

## What Was Completed
- Fixed GITHUB_TOKEN issue in Vercel — token was pointing to wrong repo
- Dashboard fully working at https://sf-vibe-dashboard.vercel.app
- Dynamic repo fetching — all sf-vibe repos auto-appear in sidebar (using `/user/repos` authenticated endpoint)
- Currently shows: sf-ps-vibe-config, sf-vibe-coding-project, sf-vibe-dashboard

## Current State
- Dashboard shows: Open Issues, Closed Issues, Total Commits, Issues list, Recent Commits
- All sf-vibe repos dynamically loaded from GitHub API via secure Vercel proxy
- GitHub token scoped to all repositories, read-only (Contents, Issues, Metadata)

## Security Status
- HTTPS ✅ (Vercel automatic)
- Token hidden in Vercel env vars ✅
- Dashboard URL publicly accessible ❌ — needs authentication
- IP restriction attempted but failed (Vercel middleware pass-through doesn't work for non-Next.js)
- Decision: implement GitHub OAuth login (no third party, just GitHub + Vercel)

## Next Session — Implement GitHub OAuth
1. Create GitHub OAuth App in GitHub Settings → Developer Settings → OAuth Apps
2. Create `api/auth/login.js` — redirects to GitHub OAuth
3. Create `api/auth/callback.js` — handles callback, sets session cookie
4. Add auth check to frontend — redirect to login if not authenticated
5. Only allow `yasushi-dotcom` GitHub account
6. Add `GITHUB_CLIENT_ID` and `GITHUB_CLIENT_SECRET` to Vercel env vars

## Open Items
- Fine-tune dashboard content and layout iteratively
- Update CLAUDE.md for sf-vibe-dashboard commit message language (English, revisit later)
- Phase 2: port to Salesforce Experience Cloud as showcase
