# 2026-03-19

## What Was Built
- Created `sf-vibe-dashboard` repo (public, React + Vite + Shadcn/ui + Tailwind)
- Basic dashboard showing Open Issues, Closed Issues, Total Commits, Issues list, Recent Commits
- Vercel serverless proxy `api/github.js` to secure GitHub token
- Deployed to https://sf-vibe-dashboard.vercel.app

## Pending / Blocked
- GitHub PAT token issue — `401 Bad credentials` error on Vercel
- Root cause: token was regenerated on GitHub but new value not yet saved in Vercel env vars
- **Next step when back:** Go to github.com/settings/tokens?type=beta → click `sf-vibe-dashboard` token → copy the current token value → paste into Vercel → Settings → Environment Variables → GITHUB_TOKEN → Save → Redeploy

## Key Decisions
- Using Vercel (free Hobby plan) instead of GitHub Pages — supports private repos
- GitHub API calls routed through Vercel serverless function to hide token from browser
- PAT scoped to `sf-vibe-coding-project` only with Contents + Issues + Metadata read-only
- Dashboard content to be fine-tuned iteratively

## Open Items
- Fix GITHUB_TOKEN in Vercel (blocked — user stepped away)
- Add more sf-vibe repos to the sidebar as they are created
- Update CLAUDE.md for sf-vibe-dashboard commit message language (English, revisit later)
- Phase 2: port to Salesforce Experience Cloud as showcase
